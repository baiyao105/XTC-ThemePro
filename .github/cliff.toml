[changelog]
body = """
## 🎨 XTC-ThemePro {% if version %} ({{ version }}){% endif %}

{% for group, commits in commits | group_by(attribute="group") %}
{% if commits | length > 0 %}
### {{ group }}
{% for commit in commits | unique(attribute="message") %}
- {% if commit.scope %}{{ commit.scope }}: {% endif %}{{ commit.message | upper_first }}
  {% if commit.remote.username %}by @{{ commit.remote.username }}{% endif %}
  {% if commit.remote.pr_number %} in [#{{ commit.remote.pr_number }}](https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/pull/{{ commit.remote.pr_number }}){% endif %}
{% endfor %}
{% endif %}
{% endfor %}

{% if version and previous.version %}
**完整变更日志**: https://github.com/{{ remote.github.owner }}/{{ remote.github.repo }}/compare/{{ previous.version }}...{{ version }}
{% endif %}
"""
footer = "<!-- generated by git-cliff -->"
output = "CHANGELOG.md"
trim = true

[git]
commit_parsers = [
  {group = "◀️ 回滚的功能", message = "^revert"},
  {group = "✨  新增功能与优化", message = "^feat|^perf"},
  {group = "🐛 Bug 修复", message = "^fix"},
  {message = "^chore|^ci", skip = true},
  {message = "^doc|^style|^test|^refactor", skip = true}
]
conventional_commits = true
filter_commits = true
filter_unconventional = true
limit_commits = 50
link_parsers = []
protect_breaking_commits = false
sort_commits = "oldest"
split_commits = false
topo_order = false
